<Window x:Class="LibraryManagement.Views.GenresWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление жанрами" Height="400" Width="500" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,5">
            <Button Content="Добавить" Command="{Binding AddGenreCommand}" Padding="10,5" Margin="5"/>
            <Button Content="Редактировать" Command="{Binding EditGenreCommand}" CommandParameter="{Binding SelectedItem, ElementName=GenresGrid}" Padding="10,5" Margin="5"/>
            <Button Content="Удалить" Command="{Binding DeleteGenreCommand}" CommandParameter="{Binding SelectedItem, ElementName=GenresGrid}" Padding="10,5" Margin="5"/>
        </StackPanel>
        <DataGrid x:Name="GenresGrid" Grid.Row="1" ItemsSource="{Binding Genres}" SelectedItem="{Binding SelectedGenre}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="250"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
<Window x:Class="LibraryManagement.Views.AuthorEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование автора" Height="250" Width="400" WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Имя:" Grid.Row="0" Grid.Column="0" Margin="5"/>
        <TextBox Text="{Binding FirstName}" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Фамилия:" Grid.Row="1" Grid.Column="0" Margin="5"/>
        <TextBox Text="{Binding LastName}" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Дата рождения:" Grid.Row="2" Grid.Column="0" Margin="5"/>
        <DatePicker SelectedDate="{Binding BirthDate}" Grid.Row="2" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Страна:" Grid.Row="3" Grid.Column="0" Margin="5"/>
        <TextBox Text="{Binding Country}" Grid.Row="3" Grid.Column="1" Margin="5"/>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Сохранить" Command="{Binding SaveCommand}" Padding="10,5" Margin="5" IsDefault="True"/>
            <Button Content="Отмена" Command="{Binding CancelCommand}" Padding="10,5" Margin="5" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>